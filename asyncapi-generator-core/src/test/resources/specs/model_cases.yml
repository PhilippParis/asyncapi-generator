asyncapi: 2.6.0
info:
  title: service
  version: 1.0.0
channels: {}
components:
  schemas:
    SomeClass:
      type: object
      properties:
        camelCase:
          type: integer
          format: int32
        PascalCase:
          type: integer
          format: int32
        snake_case:
          type: integer
          format: int32
        kebab-case:
          type: integer
          format: int32
